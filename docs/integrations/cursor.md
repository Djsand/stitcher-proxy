# Cursor Integration

You can use Stitcher Proxy as a custom OpenAI-compatible endpoint in Cursor.

## Setup

1. Open Cursor Settings
2. Go to **Models**
3. Under **OpenAI Base URL**, enter `http://localhost:8081/v1`
4. Toggle on the models you want to use.
