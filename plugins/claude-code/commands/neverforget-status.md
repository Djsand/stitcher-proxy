---
description: "Show NeverForget proxy status and memory stats"
allowed-tools: ["Bash(curl:*)", "Bash(neverforget:*)"]
---

# NeverForget Status

Check the proxy status and show memory statistics:

```!
neverforget status
```

If the proxy isn't running, start it:

```!
neverforget start &
```
